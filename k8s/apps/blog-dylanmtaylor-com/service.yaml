apiVersion: v1
kind: Service
metadata:
  name: blog-dylanmtaylor-com
  namespace: dylanmtaylor
  labels:
    app: blog-dylanmtaylor-com
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: blog-dylanmtaylor-com
