apiVersion: v1
kind: Service
metadata:
  name: apps-dylanmtaylor-com
  namespace: dylanmtaylor
  labels:
    app: apps-dylanmtaylor-com
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: apps-dylanmtaylor-com
